name: 'equity_risk'
version: '1.0.0'
config-version: 2

profile: 'equity_risk'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  equity_risk:
    staging:
      +schema: STAGING
      +materialized: view
    core:
      +schema: CORE
      +materialized: table
    marts:
      +schema: MARTS
      +materialized: table